- View Controller
  -  화면 **하나**를 관리 
   - 복잡한 레이아웃의 경우 여러개의 view controller가 들어갈 수 있음 


- ios 앱은 기본적으로 이벤트 발생 단위로 작동 (대부분의 시간 이벤트를 기다리고 있음)
- 버튼 클릭 -> 뷰 컨트롤러 호출 -> 뷰 컨트롤러가 버튼한테 메세지 전송 



- outlet -> 안드로이드에서 layout의 아이템을 자바 파일에 선언시켜주는 것과 비슷 
- var 보다는 let을 많이 사용해라! 
- 변수의 타입을 알고 싶을 땐 ? opt +클릭 
- 변수 타입 추론
- 아이폰은 세가지 이미지 해상도 사용 1x 2x 3x 
